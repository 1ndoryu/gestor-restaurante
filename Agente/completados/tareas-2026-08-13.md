# Tareas completadas — 2026-08-13

## 128A-1 — Independencia total del BDP (F0–F10, completado)

- **Qué:** la Aplicación Web queda 100% operativa **con o sin BDP**. Bloque completo F0–F10
  implementado en rama `glory-rs-rest` con gate `task:check 128A-1 --full` PASS en F7/F8/F9.
  Commits: `821954c0` (F0/F1), `92dd4cfe` (F2), `978dd3f4` (F3), `624cc9f1` (F4), `24a22b64` (F5),
  `acf59e77` (F6), `17cc1a03` (F7), `3fc17534` (F8), `e12b3968` (F9).
- **F0 — Auditoría:** inventario A/B del estado real de la integración y planificación del modo
  `standalone` (plan `plan-independencia-bdp-2026-08-12.md`).
- **F1 — Conmutador de modo:** `modo_operacion` (`bdp`|`standalone`) + invariantes M1 + badge
  «independiente» + degradación automática. **Deuda declarada (F10-1):** M2 (histéresis) y M3
  (cache TTL/invalidación) no están implementados; M1 no aplica en todos los caminos de
  escritura/polling (hallazgo F0/F1-1, ALTA). Pendiente de una fase F1.2.
- **F2 — Catálogo local:** `origen`/`local_dirty`, CRUD sin BDP, `resolve_article` (M5) e import
  M6/M7 sin pisar ediciones locales.
- **F3 — Stock local:** ajuste manual con auditoría, `GetStock`/`GetListStock` (N6), UI con origen.
- **F4 — Anulación local:** modalidades D4, reglas M8–M11, desbloqueo delete D5, auditoría.
- **F5 — Compras locales:** CRUD albaranes + conciliación local (M18), flags solo bdp (M12),
  IVA por línea (A10).
- **F6 — Historial/pagos/factura:** `origen_operacion` (A11), pagos parciales locales (A8/M13) y
  factura local mínima (A7/D9).
- **F7 — Menús/packs locales:** CRUD con líneas sobre catálogo local + UI Explorador (D2/A12/M12).
- **F8 — Permisos operativos:** 4 permisos por acción con enforcement backend (D8/M17):
  `permisos_catalogo_edicion`, `permisos_stock_ajuste`, `permisos_albaranes_gestion`,
  `permisos_anulacion_ventas` (`admin`|`admin_trabajador`|`todos`, CHECK en BD), guard
  `verificar_permiso` (403), tests 13/13 y UI en Configuración. Sin gatear sync-prices/sync-tables/
  bdp-payment/bdp-invoice: siguen protegidos por guards BDP existentes.
- **F9 — Pruebas con/sin BDP:** suite standalone completa PASS (exit 0), simulador Python 92/92,
  integración Rust contra simulador 24/24, regresión `task:check 128A-1 --full` PASS con reporte
  reproducible (`.quality-reports/branches/glory-rs-rest--f100af0a041e6e8a/128A-1/latest.md`).
- **F10 — Cierre documental:** roadmap con 128A-1 completado, feature-flags con
  `modo_operacion`/permisos, mapeo visual y guía del cliente con «Modo independiente y permisos
  operativos», plan movido a `Agente/planes/completados/`.
- **Archivos:** backend (`src/`, `migrations/`), tests (`tests/bdp_f*.rs`), frontend
  (`frontend/src/{api,componentes,hooks}`), docs (`Agente/`), roadmap.
- **Gotchas:** gate Sentinel con cuota de targets (15 GiB) que bloquea si el target en escritura
  reciente excede la cuota — liberar borrando `.pdb` del `debug/`; `--allow-heavy` con motivo
  auditable para saltar el cooldown de pesadas; clippy `-D warnings` obligatorio.
- **Sentinel:** etapa sentinel del gate PASS (0 errores) en F7/F8/F9.
- **GLORY:** no aplica; cambios del bloque en `glory-rs-rest`.
- **Pendiente de autorización del usuario:** deploy a producción y escrituras al BDP real
  (no autorizados en este bloque; SSH prohibido siempre). Pasada de `supervisor_reviewer` ejecutada
  según la skill `supervisor-review` (sin subagente disponible en el entorno): veredicto
  **AUTORIZADO PARA CONTINUAR**, documentado en `Agente/completados/128A-1-F10-cierre-documental.md`.
