# Cierre Q2.2 — read-modify-write de ModifyArticle (2026-09-06)

## Resultado

- `src/services/bdp_push.rs` completa el `ArticleData` parcial de la cola mediante `GetArticle` antes de `authorize()`.
- El merge conserva los campos remotos no enviados por Glory y sobrescribe únicamente las claves del patch local.
- `ProfilesList` y `AllProfiles` se conservan desde el payload original.
- El mismo payload enriquecido se usa para la auditoría y para `ModifyAndUpdateProfiles`.
- Si `GetArticle` falla antes de la autorización, `BdpWriteGuard::cancelar_armado_push` elimina el armado creado por el worker y devuelve la configuración a `read_only`.
- No se ejecutó ninguna escritura contra el BDP real.

## Evidencia

- `npm run fmt:check`: PASS.
- `node scripts/run-cargo.mjs test --lib article_data_merge_tests`: PASS, 2/2.
- `node scripts/run-cargo.mjs test --test bdp_push flush_modificar_articulo -- --nocapture`: PASS, 2/2.
  - Payload parcial → `GetArticle` completo → `ModifyAndUpdateProfiles` con `Price5`/`DeptCode` conservados y cambios locales aplicados.
  - `GetArticle` inválido → error visible, cero armado huérfano y modo `read_only`.
- Suite del simulador: PASS, 37/37 con `--include-ignored`.
- `cargo check`: PASS.
- `git diff --check`: PASS.

## Limitaciones

- `cargo clippy` no cierra por un defecto preexistente en `src/services/bdp_backup.rs:482` (`auditar_escritura_directa`, `clippy::too_many_arguments`) y warnings asociados. No se mezcló su reparación con Q2.2.
- `npm run quality:doctor` pasa, pero `npm run quality:lock -- --check` y `npm run gate:check -- 049A-1` quedan bloqueados por la configuración preexistente del gate: el `sourcePath` externo de Sentinel apunta a `902c45e...` mientras `quality-tools.json` fija `0559576...`; además, `provisionPath` externo es rechazado por `stages.mjs`. No se repineó ni reconstruyó Sentinel dentro de Q2.2.
- El test WireMock y el simulador no prueban la implementación concreta del BDP real. La escritura real de modificación permanece pendiente de autorización explícita y de una ventana controlada.

## Alcance de cambios

El checkout tenía cambios previos ajenos en otros archivos antes de este bloque. Se preservaron y no se usó `git add .`, `git add -A`, reset, stash ni push.
