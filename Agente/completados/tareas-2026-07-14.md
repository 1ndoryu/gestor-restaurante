# Tareas completadas — 2026-07-14

## 147A-1 — Fase 4.1: Campo bdp_order_status + migración
- **Que:** Migración `20260714300000_bdp_order_status.up.sql` que añade `bdp_order_status TEXT` a tabla `ventas` + `bdp_poll_interval_secs INTEGER NOT NULL DEFAULT 60` a `configuracion_restaurante`. Índice `idx_ventas_bdp_poll` para queries de polling eficiente. Modelos `Venta`, `VentaConCliente` y `ConfiguracionRestaurante` actualizados. `ActualizarConfiguracionRequest` con campo nuevo. Repository `configuracion.rs` actualizado para COALESCE del nuevo campo en UPDATE.
- **Archivos:** `migrations/20260714300000_bdp_order_status.{up,down}.sql`, `src/models/venta.rs`, `src/models/configuracion.rs`, `src/repositories/configuracion.rs`
- **Gotchas:** Conversión de `create()`, `find_by_id()`, `update()` en `venta.rs` de macro `query_as!` a `query_as::<_, Venta>` dinámico para evitar dependency en SQLX_OFFLINE cache. Esto es intencional — el macro requiere `.sqlx/` actualizado que no tenemos en este template.
- **Sentinel:** no aplica
- **GLORY:** No aplica

## 147A-2 — Fase 4.2: Servicio de polling BDP
- **Que:** Nuevo servicio `services/bdp_order_poller.rs` con `BdpOrderPollerService`. `poll_pending()` consulta ventas con `bdp_synced=true` y `bdp_order_status` no final, ejecuta GetOrder para cada una, mapea el status integer (0=pending, 1=accepted, 2=cancelled, 3=invoiced) y actualiza en BD. Usa `BdpWeblinkClient::new(config)` y `BdpGetOrderRequest` con `BdpOrderIdentifier::by_order_id()`. Test unitario `test_map_status()` cubre los 5 casos.
- **Archivos:** `src/services/bdp_order_poller.rs` (nuevo, ~170 líneas)
- **Gotchas:** `BdpWeblinkClient` tiene lifetime `<'a>` — el parámetro `client` en `check_order_status` necesita `BdpWeblinkClient<'_>`. Constructor es `::new()` no `::from_config()`.
- **Sentinel:** no aplica
- **GLORY:** No aplica

## 147A-3 — Fase 4.3: Endpoints BDP status + poll
- **Que:** Dos nuevos endpoints: `GET /api/ventas/:id/bdp-status` — consulta y actualiza el estado BDP de una venta individual (si tiene order_id y BDP configurado, hace polling). `POST /api/ventas/bdp-poll` — ejecuta polling para todas las ventas pendientes del usuario. Response types: `BdpOrderStatusResponse` y `BdpPollResponse` con utoipa ToSchema.
- **Archivos:** `src/handlers/ventas.rs` (3 nuevas funciones + 2 structs + 2 rutas), `src/handlers/mod.rs` (schemas OpenAPI)
- **Gotchas:** `AppError::Database` espera `sqlx::Error` no `String` — usar `?` directo con `#[from]`. Route ordering: `/ventas/bdp-poll` (POST) va DESPUÉS de `/ventas/:id/*` para evitar conflictos con Axum matchit.
- **Sentinel:** no aplica
- **GLORY:** No aplica

## 147A-4 — Registro de módulos en services/mod.rs
- **Que:** Registrados módulos BDP (`bdp_order_poller`, `bdp_sync`, `bdp_sync_preflight`, `bdp_weblink`, `bdp_weblink_catalog`) y dominio mínimo (`configuracion`, `haddock`, `venta`) en `services/mod.rs` con sus `pub use` exports. Los módulos glory-rs (ai_chat, billing_stripe, etc.) permanecen declarados pero sin archivos — son errores preexistentes del template. Modificados `bdp_weblink` y `bdp_order_poller` a `pub(crate)` para acceso desde handlers.
- **Archivos:** `src/services/mod.rs`
- **Gotchas:** No agregar TODOS los módulos de dominio de golpe (api_key, campana, etc.) — tienen dependencias en campos que no existen en el template (UserRole, Claims.role, AuthUser.trabajador_id). Solo los necesarios para BDP.
- **Sentinel:** no aplica
- **GLORY:** No aplica

## 147A-5 — Fixes de compilación BDP + fixtures de tests
- **Que:** Corregidos errores de compilación en código BDP: `venta.user_id` es `Uuid` no `String` (bdp_sync.rs resolve_customer), `cliente.telefono` es `String` no `Option` (filter → is_empty check), `BdpWeblinkClient` lifetime `<'_>`, constructor `::new()` no `::from_config()`. Fixtures de tests actualizadas en haddock.rs, bdp_sync.rs, bdp_sync_preflight.rs, bdp_weblink.rs para incluir campos BDP nuevos (`bdp_order_status`, `bdp_tender_map`, `bdp_order_type_map`, `bdp_default_customer_code`, `bdp_poll_interval_secs`, `bdp_default_article_code`, `bdp_default_article_name`).
- **Archivos:** `src/services/bdp_order_poller.rs`, `src/services/bdp_sync.rs`, `src/handlers/ventas.rs`, `src/services/haddock.rs`, `src/services/bdp_weblink.rs`, `src/services/bdp_sync_preflight.rs`
- **Gotchas:** `ActualizarConfiguracionRequest` NO tiene `bdp_default_article_code` ni `bdp_default_article_name` — esos campos son solo en `ConfiguracionRestaurante`. No mezclar.
- **Sentinel:** no aplica
- **GLORY:** No aplica

## 147A-F6 — Fase 6: Frontend multi-item y formulario de ventas
- **Que:** Implementado editor de líneas de venta multi-item en frontend. 9 archivos creados/modificados: esquemas `CrearVentaLineaRequest` y campo `lineas` en `CrearVentaRequest`, hook `useLineasVenta` (gestión de líneas CRUD + totales), componente `LineasVentaEditor` (tabla responsive mobile/desktop con BDP mapping badge), componente `ArticleAutocomplete` (selector con búsqueda en bdp_article_map), integración en `FormularioVenta` (toggle para añadir artículos individuales), API `useBdpArticleMaps` con query hook. Hooks refactorizados para cumplir límites Glory Sentinel: `useFormularioVenta` (202→53 líneas) con extracción a `ventaFormUtils.ts` (62 líneas) y `useVentaSubmit.ts` (90 líneas). Iconos lucide en vez de emojis Unicode.
- **Archivos:** `frontend/src/api/generated/gestionRestauranteAPI.schemas.ts`, `frontend/src/api/bdp.ts`, `frontend/src/hooks/useLineasVenta.ts`, `frontend/src/hooks/ventaFormUtils.ts`, `frontend/src/hooks/useVentaSubmit.ts`, `frontend/src/hooks/useFormularioVenta.ts`, `frontend/src/components/article-autocomplete.tsx`, `frontend/src/components/lineas-venta-editor.tsx`, `frontend/src/componentes/FormularioVenta.tsx`
- **Gotchas:** Orval codegen sigue bloqueado (176 errores pre-existentes impiden `dump_openapi`). Tipos de API (`CrearVentaLineaRequest`, `BdpArticleMapItem`) añadidos manualmente. `article-autocomplete.tsx` tiene warnings Sentinel pre-existentes (botón nativo, dropdown artesanal) — aceptados por ahora.
- **Sentinel:** no aplica (warnings pre-existentes en article-autocomplete)
- **GLORY:** No aplica


## 147A-2 — BDP Phase 5: Frontend visibilidad BDP completa
- **Que:** Implementacion completa de Fase 5 del plan BDP. Orval codegen fixes (3 schemas). Filtro estado_bdp en backend. Retry BDP sync button wired through ListaVentas. Nuevo componente BdpArticleMapTable con CRUD + import catalogo. Nuevo endpoint POST /api/bdp/article-maps/import-catalog. ConfigBdpMapeos integrado.
- **Archivos:** 18 archivos modificados/creados (backend handlers, models, repositories, services + frontend hooks, components, Orval generated)
- **Gotchas:** BdpWeblinkClient lifetime, AppError::Internal (no External), ConfiguracionRestaurante usa strings no-Option
- **Sentinel:** no aplica
- **GLORY:** No aplica
