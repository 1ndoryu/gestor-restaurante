# Tareas cerradas — 2026-09-06

## Bloque 039A-1 / F3 — cierre documental de Q1 y validación UI

- **Rama operativa confirmada:** `main`. Se alinearon `sentinel.config.json`, `roadmap.md`,
  los planes activos de revisión/lecturas/escrituras y las referencias de checkout usadas por
  deploy/compose. No se renombró ninguna rama ni se hizo push.
- **Lecturas Q1 desde la interfaz:** validadas sin acciones de escritura en Plano de Sala,
  Stock, Compras, Historial y Sincronización. También quedaron confirmados el badge `BDP: lectura`,
  la configuración de solo lectura y la ausencia de controles de escritura disponibles en ese
  estado.
- **Evidencia UI:**
  - Plano de Sala: carga correcta; `Sync BDP` no se ofrece con el modo efectivo de lectura.
  - Stock: artículos y existencias visibles; no se pulsó `Ajustar` ni ningún control de sincronización.
  - Compras: cuatro albaranes visibles en modo demo; no se pulsó `Sync`.
  - Historial: cuatro registros de auditoría y dos snapshots visibles.
  - Sincronización: 15 filas, 3 sincronizadas; operaciones pendientes y errores NRE de W-Q2.1
    visibles. No se pulsó `Sincronizar ahora` ni `Reintentar`.
- **Seguridad:** cero escrituras nuevas contra el BDP durante esta validación; no se hizo flush,
  reintento, snapshot nuevo, deploy ni push.
- **Hallazgo diferido:** `sync-prices` requiere una comprobación separada del endpoint y de su
  respuesta completa. La observación previa (`HTTP 200`, errores y cero actualizados) no se usa
  como PASS ni se modifica en este cierre.
- **Cambios de código incluidos en el bloque:**
  - `src/services/bdp_backup.rs`: fallback específico para `[200109]` mediante el catálogo del
    perfil, conservando el fallo alto para errores distintos y para fallback vacío.
  - `src/services/bdp_weblink_catalog.rs`: serialización de entradas de precios/artículos para
    reutilizar los datos en el flujo de sincronización.
- **Verificación funcional previa registrada:** `sync-catalog` contra el BDP real respondió 200
  con 557 artículos, 0 errores, usando el fallback de perfil tras `[200109]`.
- **Pendiente posterior:** bloque 2, escrituras reales Q2 una por una. Q2.2 permanece bloqueada
  hasta revisar el contrato completo de `ModifyArticleAndUpdateProfile`; el BDP devuelve una NRE
  con el payload mínimo actual. Pagos, factura y cancelación siguen bloqueados por la suscripción
  WebLink de pago.
