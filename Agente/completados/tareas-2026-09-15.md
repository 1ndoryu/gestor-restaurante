# Tareas 2026-09-15 — W-Q2.2 ejecución real + verificación

## W-Q2.2 Modificar artículo (neutralizar `90000003`) — VERDE con limitación

- Qué: desactivación del artículo de prueba `90000003` en BDP real mediante
  `activo=false` → `WebArticle:false` (vía de neutralización; sin DeleteArticle).
  Autorización explícita del usuario 2026-09-15 ("Neutralizar 90000003").
- Archivos: `src/services/bdp_push.rs` (`web_article: Some(map.activo)` +
  `article_data_merge_tests` 2 tests);
  `Agente/planes/plan-seguridad-escrituras-bdp-2026-09-14.md` (§5 marcado, §8 bitácora).
- Ejecución real (BD `glory_backend_kamples`):
  - Cola `5bb50762-…` `articulo/modificar` → `sincronizado` 2026-09-15T06:18:54Z, reintentos 0.
  - Audit `modify_article` `exito` 06:18:51Z; payload con `"WebArticle": false`;
    respuesta `{"ErrorMessage": "", "ListaErroresArticulo": []}`.
- Verificación de vuelta 07:17–07:18Z (backend local 3100 contra kamples):
  - `GET /api/bdp/explorar`: 0 artículos, 5 clientes, 0 departamentos plano,
    7 salones, 3 empleados.
  - `GET /api/bdp/departamentos/perfil` (ExportFromProfile): 66 departamentos,
    `Codigo 1 = CAFES` presente (perfil intacto).
  - `POST /api/bdp/backup/completo` → snapshot `811f4a15-…`: `Articles: []` plano.
- Evidencia/tests: `node scripts/run-cargo.mjs build --bin glory-backend` OK;
  `test --lib article_data_merge_tests` 2/2; `test --test bdp_push
  flush_modificar_articulo` 2/2 (sesión actual).
- Gotchas:
  - `run-with-db` en `main` apunta a `glory_backend` (vacía, 0 usuarios); los
    datos BDP viven en `glory_backend_kamples` (58 usuarios, bootstrap aplicado).
    Backend levantado con `DATABASE_URL` a kamples + `PORT=3100`.
  - dotenvy no expuso `BDP_DEFAULT_ARTICLE_NAME` al hijo `Start-Process`
    (clave sana en `.env`); se pasó explícita desde `.env` sin tocar el archivo.
  - `ConvertFrom-Json` falla con claves duplicadas `TAVPer/TavPer` del audit:
    usar `-AsHashtable` o `strpos` en SQL.
- Limpieza: backend detenido (PID 23820), token borrado, sin cambios en BDP
  salvo la neutralización autorizada; `git status` solo `M src/services/bdp_push.rs`.
- Limitación: no hay endpoint `GetArticle` directo, así que no se distingue
  "despublicado (`WebArticle:false`)" de "oculto por otro motivo". Evidencia a
  favor de despublicación: payload enviado con `WebArticle:false` + `exito` sin
  errores + plano vacío + perfil intacto. `GetArticle` directo queda como
  verificación opcional futura (requeriría endpoint nuevo; fuera de alcance).
- Sentinel: sin cambios; gate no ejecutado en este bloque (bloqueo preexistente
  documentado en `tareas-2026-09-06-q2.2.md`).
- Pendientes reales: siguiente W 1 por 1 solo limpiable; W-Q2.5/Q2.6/Q2.11
  requieren suscripción activa verificada.

## W-Q2.3 Alta departamento (`PRUEBA-149A2-DEP-20260915`) — ENVIADO, BDP no lo creó (sin residuo)

- Qué: alta del departamento de prueba autorizado 2026-09-15 ("Sí, crear
  departamento", aviso permanente aceptado). Flujo completo del worker en verde
  (armado → auditoría → envío → `exito`), pero el BDP devolvió éxito con avisos
  y el departamento NO aparece en el perfil: cero residuo en BDP.
- Ejecución real (BD `glory_backend_kamples`, backend local 3100):
  - Fila local `bdp_catalogo_clasificaciones` creada (`tipo=departamento`,
    `code=1`, `nombre=PRUEBA-149A2-DEP-20260915`, id `64fb5cef-…`).
  - Cola `25a31706-…` `departamento/crear` → `sincronizado` 2026-09-15T07:38Z,
    reintentos 0.
  - Audit `create_department` `exito` 07:38:00Z; respuesta `{"ErrorMessage": "",
    "ListaErroresArticulo": ["ShortDescription: LA LONGITUD DE LA DESCRIPCIÓN
    ABREVIADA ES INCORRECTA", "PrinterLevel: EL NIVEL DE IMPRESIÓN 0 ES
    INCORRECTO"]}` (avisos, no bloqueo).
- Verificación de vuelta: `GET /api/bdp/departamentos/perfil` (ExportFromProfile)
  19 grupos raíz; `Codigo 1` sigue siendo `CAFES`; ninguna entrada `PRUEBA-149A2`;
  `GET /api/bdp/explorar` departamentos plano `cantidad=0` (igual que antes).
- Causa raíz: el código local se asigna `MAX(code)+1` por usuario empezando en 1
  (`BdpCatalogoClasificacionRepository::siguiente_code`), y el código 1 ya existe
  en el BDP real (`CAFES`). Con `Overwrite=false` el BDP conservó el existente y
  devolvió éxito con avisos (comportamiento verificado, no documentado en manual).
- Gotchas nuevos:
  - El binario NO carga `.env` vía dotenvy al hijo `Start-Process`: el primer
    flush dio `errores=1` sin fila de auditoría porque
    `ensure_write_target_allowed` (`src/services/bdp_weblink.rs:785-811`) lee
    `BDP_WRITE_ALLOWED_ORIGINS` del entorno del proceso (`unwrap_or_default` →
    vacío → destino denegado). Relanzar con `BDP_WRITE_ALLOWED_ORIGINS` y
    `BDP_CHECK_ORDER_ALLOWED_ORIGINS` explícitos lo resolvió (`sincronizados=1`).
    Lección: arrancar el backend de pruebas con el entorno BDP completo o el
    flush falla en el armado sin dejar rastro en auditoría.
  - `payload_crear_departamento` (`src/services/bdp_push.rs:407`) envía el nombre
    completo también como `ShortDescription` (demasiado largo para BDP) y
    `PrinterLevel=0` (BDP lo rechaza como valor); el BDP los reporta como avisos.
    Mejora de payload pendiente (acortar `ShortDescription`, `PrinterLevel`
    válido) antes de reintentar con un código libre.
- Limpieza: backend detenido (PID 7172, antes 21192 sin env completo), token
  borrado, sin cambios en BDP (código 1 intacto como `CAFES`).
- Decisión pendiente del usuario: reintentar el alta con un código libre del
  perfil (p. ej. `MAX(códigos del perfil)+1`) + payload corregido (crearía un
  departamento PERMANENTE real en el perfil del cliente), o cerrar W-Q2.3 como
  "no creado por colisión de código" y seguir con el siguiente W.

## W-Q2.3 Reintento con código libre `901` — VERDE (departamento creado, permanente)

- Autorización: usuario 2026-09-15 ("Sí, reintentar con código libre").
- Cambios de código (commit de este bloque):
  - `src/services/bdp_weblink_catalog.rs`: campo nuevo `printer_level`
    (`PrinterLevel`) en `BdpCreateDepartmentRequest` y
    `BdpCreateDepartmentProfilesRequest`.
  - `src/services/bdp_push.rs`: `payload_crear_departamento` trunca
    `ShortDescription` a 10 caracteres y fija `PrinterLevel=1` (manual WebLink:
    abreviada máx 10, nivel 1–9).
  - `src/services/bdp_weblink.rs`: `create_department` y
    `create_department_and_update_profiles` validan `ensure_embedded_errors_empty`
    (igual que artículos H-W). Sin esto, el rechazo "YA EXISTE" con HTTP 200 +
    `ErrorMessage` vacío se marcaba como `exito` (falso positivo real observado).
  - `tests/bdp_push.rs`: aserciones `ShortDescription`/`PrinterLevel` + truncado;
    `tests/bdp_simulator_integration.rs`: `printer_level: 1` en los 2 sitios.
- Secuencia real: intento código 1 (07:38Z, `exito` con 2 avisos, no creado —
  colisión con `CAFES`); intento código 104 (07:47Z, `exito` con aviso "EL
  DEPARTAMENTO 104 YA EXISTE", no creado — el perfil solo muestra hasta 103,
  hay departamentos fuera del perfil); reintento código `901` (07:54Z):
  cola `0b527f18-…` → `sincronizado`, audit `create_department` `exito` con
  `{"ErrorMessage": "", "ListaErroresArticulo": []}` (cero avisos: fix payload
  confirmado).
- Verificación de vuelta: ExportFromProfile 55 departamentos con
  `901=PRUEBA-149A2-DEP-20260915`. Residuo PERMANENTE autorizado (sin Delete).
- Evidencia/tests: `build --bin glory-backend` OK; `test --test bdp_push
  payload_crear_departamento` 1/1; `test --test bdp_simulator_integration
  department -- --include-ignored` 2/2.
- Lección para siguientes W de catálogo: el código local (`MAX+1` desde 1)
  colisiona con códigos reales del BDP; el BDP responde `exito` con avisos en
  vez de rechazar (ahora detectado por `ensure_embedded_errors_empty` →
  `rechazado`/`error` honesto en vez de falso `exito`).
- Limpieza: backend detenido, token borrado.
- Pendientes reales: siguiente W 1 por 1 (W-Q2.4 comanda); W-Q2.5/Q2.6/Q2.11
  requieren suscripción activa verificada.
