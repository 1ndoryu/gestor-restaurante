# Tareas completadas — 2026-09-18

## 169A-2 P2 — Venta real mínima con pago-en-creación + verificación empírica de CancelOrder (CERRADO)

### Qué se hizo
- **P2 real** (autorización explícita del usuario, hueco muerto): venta
  `PRUEBA-169A2-PAGO-20260917` (0,10 € base + 0,01 € IVA, `1001 CAFE BOMBON`,
  `efectivo`, turno noche/comedor) con `Payments` dentro del `CreateOrder` inicial
  e `Invoice=false` (seam temporal `BDP_P2_SIN_FACTURA=1`, ya retirado) →
  **comanda BDP 6338** (`bdp_synced=true`, `bdp_sync_error=null`,
  `bdp_synced_at=2026-09-18T01:39:58Z`). Venta local
  `f3bfa1d5-be81-4737-a87a-3d2c832e055c`.
- **CancelOrder verificado funcionando** (el usuario tenía razón; refuta el
  "Subscripción no activada" de 2026-08-05): anulación local de la venta +
  `POST /api/bdp/push/7f6bb734-…/reintentar` (reintento individual, NO flush
  global: la cola trae 10 filas ajenas con errores — stock/almacén, artículo/IVA —
  y el flush global las habría reenviado) → fila `venta/6338/cancelar` =
  **`sincronizado`**, auditoría `cancel_order` = **`exito`**
  (`cfde8212-74b7-4e55-b4dc-dbaccb8d99fa`, 2026-09-18T07:30:41Z).
- **Limpieza**: seam `BDP_P2_SIN_FACTURA` retirado de `bdp_sync_venta.rs` + su
  test eliminado de `bdp_sync.rs`; comentario gotcha de `CancelOrder` corregido;
  textos de `ConfigBdp.tsx` (badge/tooltip) actualizados a "Verificado en BDP
  2026-09-18"; filas vivas del roadmap corregidas (resumen BDP, 169A-2, 149A-2,
  149A-3, 247A-9 D5, pendientes #3). El historial fechado se conserva intacto.
- Config restaurada tras cada operación: `sync_mode=read_only`, `modo=standalone`,
  `sync_enabled=false`, `bdp_tender_map={}`.

### Archivos
- `src/services/bdp_sync_venta.rs` (seam retirado, `invoice: Some(pago_incluido)` restaurado)
- `src/services/bdp_sync.rs` (test P2 eliminado, gotcha CancelOrder corregido)
- `frontend/src/componentes/ConfigBdp.tsx` (badge/tooltip verificados)
- `roadmap.md` (cierre 169A-2 + filas CancelOrder/pagos)
- Preexistentes de la sesión (P0/P1/dry-run, sin commit): `src/handlers/configuracion.rs`
  (`?tender_id` en `sync-dry-run`), `src/services/bdp_sync_preflight.rs` (P1 OnlyCheck con pago)

### Evidencia / tests / gate
- `C:\tmp\glory-p2.json` (6338 synced), `C:\tmp\glory-p2-status.json`,
  `C:\tmp\glory-p2-cancel.json` (reintento `sincronizados:1`), `C:\tmp\glory-p2-cola.json`
- `cargo test --lib bdp_sync`: **44 passed / 0 failed**; `cargo clippy --lib -- -D warnings`: limpio
- Venta final vía API: `anulada=true` (motivo+usuario registrados), `bdp_invoiced=false`

### Decisiones / gotchas
- Reintento **individual** (`POST /api/bdp/push/:id/reintentar`, modo `bdp`) en vez de
  flush global: el flush reenvía también las filas ajenas en `error`.
  `reintentar_uno` se auto-arma (`cancel_order`→`venta/cancelar`), no necesita `PUT sync-mode`.
- `PUT /api/configuracion/bdp/sync-mode` con `alcances=["cancel_order"]` +
  `target_entity_type=venta` da **422** ("El objetivo no coincide…"): el validador
  objetivo/alcance no clasifica `cancel_order` como venta aunque el push lo usa.
- `GET /api/bdp/push/pendientes` devuelve hasta 100 filas sin filtrar por estado
  (incluye `sincronizado`); filtrar por `id` en el cliente.

### Prevención / Sentinel
- Sin cambios al gate. Proyecto declara Sentinel 0.7.1; cierre con tests+clippy verdes.

### Pendientes reales (nuevos, no iniciados)
- Permitir armado manual `cancel_order` en `PUT sync-mode` (añadir al match
  `sale_only` de `configuracion.rs:388` + test) y decidir con el cliente si se
  retira el flag `ff_bdp_cancel_order` (el backend no lo consulta: el push
  cancela siempre al anular).
