# Tareas completadas — 2026-09-18

## 169A-2 P2 — Venta real mínima con pago-en-creación + verificación empírica de CancelOrder (CERRADO)

### Qué se hizo
- **P2 real** (autorización explícita del usuario, hueco muerto): venta
  `PRUEBA-169A2-PAGO-20260917` (0,10 € base + 0,01 € IVA, `1001 CAFE BOMBON`,
  `efectivo`, turno noche/comedor) con `Payments` dentro del `CreateOrder` inicial
  e `Invoice=false` (seam temporal `BDP_P2_SIN_FACTURA=1`, ya retirado) →
  **comanda BDP 6338** (`bdp_synced=true`, `bdp_sync_error=null`,
  `bdp_synced_at=2026-09-18T01:39:58Z`). Venta local
  `f3bfa1d5-be81-4737-a87a-3d2c832e055c`.
- **CancelOrder verificado funcionando** (el usuario tenía razón; refuta el
  "Subscripción no activada" de 2026-08-05): anulación local de la venta +
  `POST /api/bdp/push/7f6bb734-…/reintentar` (reintento individual, NO flush
  global: la cola trae 10 filas ajenas con errores — stock/almacén, artículo/IVA —
  y el flush global las habría reenviado) → fila `venta/6338/cancelar` =
  **`sincronizado`**, auditoría `cancel_order` = **`exito`**
  (`cfde8212-74b7-4e55-b4dc-dbaccb8d99fa`, 2026-09-18T07:30:41Z).
- **Limpieza**: seam `BDP_P2_SIN_FACTURA` retirado de `bdp_sync_venta.rs` + su
  test eliminado de `bdp_sync.rs`; comentario gotcha de `CancelOrder` corregido;
  textos de `ConfigBdp.tsx` (badge/tooltip) actualizados a "Verificado en BDP
  2026-09-18"; filas vivas del roadmap corregidas (resumen BDP, 169A-2, 149A-2,
  149A-3, 247A-9 D5, pendientes #3). El historial fechado se conserva intacto.
- Config restaurada tras cada operación: `sync_mode=read_only`, `modo=standalone`,
  `sync_enabled=false`, `bdp_tender_map={}`.

### Archivos
- `src/services/bdp_sync_venta.rs` (seam retirado, `invoice: Some(pago_incluido)` restaurado)
- `src/services/bdp_sync.rs` (test P2 eliminado, gotcha CancelOrder corregido)
- `frontend/src/componentes/ConfigBdp.tsx` (badge/tooltip verificados)
- `roadmap.md` (cierre 169A-2 + filas CancelOrder/pagos)
- Preexistentes de la sesión (P0/P1/dry-run, sin commit): `src/handlers/configuracion.rs`
  (`?tender_id` en `sync-dry-run`), `src/services/bdp_sync_preflight.rs` (P1 OnlyCheck con pago)

### Evidencia / tests / gate
- `C:\tmp\glory-p2.json` (6338 synced), `C:\tmp\glory-p2-status.json`,
  `C:\tmp\glory-p2-cancel.json` (reintento `sincronizados:1`), `C:\tmp\glory-p2-cola.json`
- `cargo test --lib bdp_sync`: **44 passed / 0 failed**; `cargo clippy --lib -- -D warnings`: limpio
- Venta final vía API: `anulada=true` (motivo+usuario registrados), `bdp_invoiced=false`

### Decisiones / gotchas
- Reintento **individual** (`POST /api/bdp/push/:id/reintentar`, modo `bdp`) en vez de
  flush global: el flush reenvía también las filas ajenas en `error`.
  `reintentar_uno` se auto-arma (`cancel_order`→`venta/cancelar`), no necesita `PUT sync-mode`.
- `PUT /api/configuracion/bdp/sync-mode` con `alcances=["cancel_order"]` +
  `target_entity_type=venta` da **422** ("El objetivo no coincide…"): el validador
  objetivo/alcance no clasifica `cancel_order` como venta aunque el push lo usa.
- `GET /api/bdp/push/pendientes` devuelve hasta 100 filas sin filtrar por estado
  (incluye `sincronizado`); filtrar por `id` en el cliente.

### Prevención / Sentinel
- Sin cambios al gate. Proyecto declara Sentinel 0.7.1; cierre con tests+clippy verdes.

### Pendientes reales (nuevos, no iniciados)
- Permitir armado manual `cancel_order` en `PUT sync-mode` (añadir al match
  `sale_only` de `configuracion.rs:388` + test) y decidir con el cliente si se
  retira el flag `ff_bdp_cancel_order` (el backend no lo consulta: el push
  cancela siempre al anular).

## 149A-2 Fase 3 — Escrituras reales (ejecutada 2026-09-18, autorización por fase del usuario)

### Qué se hizo (dato de prueba anunciado antes de cada envío; config restaurada tras cada op)
- **W-Q2.7 propina VERDE**: `POST propina` 0,01 € `add_tip:true` sobre venta 6258
  → fila `propina/propina` `05d61e7f` pendiente; reintento en `standalone` =
  `omitidos_standalone:1` (propina/pago/stock exigen modo `bdp`); con modo `bdp`
  → 1/1 `sincronizado`; audit `add_tip` = `exito` (10:24:44Z,
  `ErrorMessage:""`). Modo `standalone` restaurado.
- **W-Q2.9 stock, canal OK pero BDP sin almacén 1**: ajustes +1/-1 sobre `1001`
  (`PRUEBA-149A2-Q2.9`, idempotencias `Q2.9-mas1/menos1-20260918`), stock local
  1.0 → 0 (neto cero); flush en modo `bdp` → `[201600]-EL ALMACÉN 1 NO EXISTE`
  (misma raíz que el `[201500]` del inventario). Solo se encoló el +1 (posible
  dedupe del -1 por misma tupla pendiente); sin efecto neto al ser rechazado.
  Nuevo punto 5 del §9 del plan (crear almacén 1).
- **W-Q2.5 re-verificado bloqueado**: armado manual `unidirectional`
  (`add_payment`↔venta 6258, snapshot vigente) + intento 0,11 € Contado
  (`Q2.5-20260918-6258`) → `422 [301010]` pre-escritura; `bdp-payments` 0,11
  pendiente / 0 pagado / 0 pagos, sin `add_payment` en auditoría; desarmado +
  `standalone`. Auto-arm rechazado (`ff_bdp_auto_arm=false`); el manual sí arma.
- **W-Q2.6 NO intentada** (correcto): sin tier de pago verificado y una factura
  exitosa sería irreversible.
- **W-Q2.11 formal**: la cancelación de la 6338 de esta noche cuenta como
  ejecución (fila `sincronizado`, audit `exito`, venta `anulada=true`).
- **W-S3.2 parcial**: parte local OK (`read_only`/`standalone`/`sync_enabled=false`,
  cola 13 filas coherente, stock `1001`=0, pagos 6258 en 0). Pendiente TPV del
  restaurante: anular 6258 + depto 901.

### Archivos
- `Agente/planes/plan-seguridad-escrituras-bdp-2026-09-14.md` (checkboxes Q2.5/Q2.7/Q2.9/Q2.11/S3.2,
  bitácora §8, informe §9 puntos 5-6, estado §7)
- `roadmap.md` (pendiente de actualizar tras este commit)

### Decisiones / gotchas
- Arranque del backend: `node scripts/run-cargo.mjs run` compiló pero el binario
  murió en bootstrap (`BDP_DEFAULT_ARTICLE_NAME es obligatorio`) con el mismo
  `.env` que en primer plano funciona; arranque bueno con el binario ya
  compilado (`C:\tmp\glory-target\debug\glory-backend.exe` + `.env` + `PORT=3100`).
  Causa del wrapper sin confirmar — no se toca código por esto.
- Fallo propio contenido: un `Where-Object` con `-and` no filtró y reintenté TODA
  la cola en modo `bdp`. Sin consecuencias: las sincronizadas las rechazó el
  servidor (422) y las viejas en error repitieron su error conocido
  (`[201500]`/`[201600]`/TAVCode), `reintentos` sin cambios. Lección: imprimir el
  conteo filtrado antes de iterar reintentos.
- `omitidos_standalone:1` (propina) y auto-arm off (pago) confirman dos puertas
  distintas: modo `bdp` para flush, armado manual para escritura directa.

### Prevención / Sentinel
- Sin cambios de código: solo docs. Cierre con estado verificado vía API, sin gate.

### Pendientes reales tras Fase 3
- **S9 visual** (contigo en pantalla) + TPV restaurante (anular 6258, depto 901,
  crear almacén 1, IP mensajes en terminal, tier de pago).
- Q2.6 factura y pago posterior: bloqueados hasta WebLink de pago.

## Sesión tarde 2026-09-18 — Deploy desde cero + imports BDP vía UI (cuenta restaurante)

### Qué se hizo
- Deploy desde cero verificado: `DROP SCHEMA public` en `rust_db` + `deploy-service
  --skip-backup` OK, `/api/health 200`, 148 migraciones, login admin 200, Dashboard
  + Configuración/BDP + Sincronización visibles.
- Rotación password demo `demo@restaurante.com` → `a3qTnFAZADSyL3nStJ7w9tv9`
  (argon2id vía base64, login UI verificado).
- Activación BDP cuenta `restaurante` (`3973fbce…`) vía `UPDATE … FROM demo` +
  `sync_enabled=FALSE poll_enabled=FALSE sync_mode='read_only'`; dry-run lectura OK.
- Imports BDP vía UI (solo lectura, cero escrituras):
  - Catálogo: 559 artículos (50/pág; ej. `10001 BARCELO`); `sync_prices` 0
    actualizados, 559× `[200500]-ARTÍCULO INCORRECTO` (rango 7005-9014).
  - Stock: `559 artículos · Última sync 18 sept 09:14` (precios visibles;
    `BDP no devuelve existencias`).
  - Inventario: `0 contados · 559 en catálogo`.
  - Clientes: preview Nuevos 1 / Inválidos 4 / Total 5 → aplicado, `clientes 0→1`.
  - Plano Sala: preview `7 zonas y 87 mesas` → aplicado (PRINCIPAL 13, T.RENFE 40,
    T.CHILL OUT 12, J.MARTINEZ 11, S.GOMEZ 9, RESERVADO HARRY 2, DELIVERY 0).
  - Sincronización: solo botón flush (export, PROHIBIDO) — no tocado.
  - Historial: vista auditoría OK. Menús y Packs: sin ruta (`/menus` → `/`) — nada que importar.
  - Ventas: 0 registros; botón `Importar del BDP` no abre modal (poll-only por diseño).
- Anomalía: `bdp_sync_enabled` volvió a `true` solo tras copiar config (sin UPDATE);
  causa sin confirmar; `configuracion_restaurante` tiene 0 triggers.

### Bloqueo Compras (requiere usuario)
- `bdp_purchase_notes_profile_id=null`: el `ExportPurchaseNotes` exige código de
  perfil numérico (el "1" probado no se guardó); solo Guillermo puede leer el
  código real en el TPV BDP.
- Bug backend: `BdpPurchaseNoteRepository::listar` compara `date >= text`
  (`params.fecha_desde/hasta Option<String>`) → 500 `operator does not exist:
  date >= text` al filtrar por fechas; requiere fix (`::date`/`NaiveDate`) + deploy
  autorizado. `bdp_purchase_notes` 0 filas.
- `ff_bdp_purchase_notes_read` pasó `f→t` tras la UI; draft/receive/auto_arm=`f`.

### Cierre fix fechas Compras (misma tarde, autorizado por el usuario)
- Fix `dfe7edd` (push `main`): `BdpPurchaseNoteListParams.fecha_desde/hasta`
  `Option<String>` → `Option<NaiveDate>` (misma convención que `reserva.rs`);
  repo bindea `NaiveDate`; utoipa actualizado. Perfil compras SIN tocar (el
  cliente lo configura cuando lo vea).
- Evidencia: `check` OK, `clippy -D warnings` OK, `test --lib` 189/189,
  `test --test bdp_backup` 27/27; `fmt --check` solo deuda preexistente en
  `bdp_sync_preflight.rs` (no tocada).
- Incidente de tooling: `cargo test` (todos los targets) fallaba con
  `failed to remove glory-backend.exe (os error 5)` — era el preview :3100
  (PID 6344, `C:\tmp\glory-target\debug\`, sesión anterior) con el fichero
  bloqueado; detenido (los de `inmobiliaria`/`harness`/`PROYECTO TASKS` no se
  tocaron) y todo verde después.
- Deploy: `deploy --name glory-rest --update --skip-backup` + `health`
  (`http_ok=true app_ok=true fatal_logs=false`).
- Verificación viva: `GET /api/bdp/purchase-notes?fecha_desde=2026-08-18&
  fecha_hasta=2026-09-18` → `200 []` (antes 500); `fecha_desde=mala` → `400`.

## Segunda guía para el cliente (misma tarde, a petición del usuario)
- `Agente/usuario/guia-cliente-segunda-entrega-bdp-2026-09-18.md`: continúa la
  guía del 19/07; resumida, no técnica, solo logros y decisiones, sin checklists
  de pruebas (ya hechas). Cubre planes post-19/07 revisados vía 3 subagentes en
  paralelo (independencia 128A-1/208A/198A, verificación real 149A/169A-2/Q1,
  resto: permisos, rendimiento, catálogo/sync, plano, servidor/SaaS, deploy).
- Próximos pasos declarados en la guía: perfil de albaranes del TPV,
  suscripción WebLink de pago (opcional), limpieza 6258/depto 901.

### Archivos
- `src/repositories/bdp_purchase_note.rs:27-50` (bug fechas),
  `src/models/bdp_purchase_note.rs:118-136`,
  `src/handlers/bdp_purchase_note.rs:618-642`,
  `frontend/src/componentes/bdp/BdpCompras.tsx`, `BdpPurchaseSyncControls.tsx`,
  `frontend/src/componentes/ListaVentas.tsx:82`, `PlanoSala.tsx:188`.

## 267A-7 - Tercer modo BDP automatico (cerrado 2026-09-18, commit 0b82cd0)
- Backend: modo utomatic en valid_modes + rama en cambiar_bdp_sync_mode
  (confirmacion + destino + backup + snapshot vigente, sin armado) + rama en
  uthorize del write-guard + helper modo_permite_escritura en los 5 gates
  de push. Clippy limpio, suite 189/189.
- Frontend: tercera tarjeta en ConfigBdp, badge 3 estados, opcion en
  PanelBdpBackup. dialogoConfirmacion.tsx sustituye todos los
  window.confirm/prompt (nativos rotos en navegador empaquetado; sonner depende
  de rAF congelado).
- Hallazgo 267A-8: el front enviaba 	arget_entity_id: '' y el backend lo
  declara Option<Uuid> -> 422 del extractor en texto plano + toast generico.
  Fix: enviar 
ull sin objetivo + mensajeErrorBackend extrae
  esponse.data.message o texto plano. Sin cambios de backend.
- Verificacion en navegador: flujo completo con cuenta local (PUT -> 422 con
  mensaje real 'falta un snapshot completo...'); modo sigue ead_only.
- Sin deploy (toca escrituras reales). Plan archivado en
  Agente/planes/completados/plan-modo-automatico-bdp-2026-09-18.md.
