# Tareas completadas — 2026-08-19

## 198A-1 — Escrituras BDP completas (F1–F9, completado)

- **Qué:** capa de escritura Glory → BDP construida sobre los datos locales de 128A-1, con
  **independencia intacta** (en `standalone` el worker no envía nada). Todo lo que BDP puede
  aceptar como escritura queda encolado y despachado con guards; las operaciones locales siguen
  funcionando sin BDP.
- **F1 — Infraestructura push:** migración `20260821000000_bdp_push_escrituras` (config
  `push_modalidad`/`bdp_tav_map`/`bdp_almacen_default`/`bdp_codreg_default`/`bdp_articulo_rango_inicial`
  + tabla `bdp_push_pendientes` con UNIQUE parcial M19) y correctiva `20260822000000_bdp_push_estado_ancho`
  (`estado` VARCHAR(30), cabe `pendiente_suscripcion`). `BdpPushService` (encolar upsert M19,
  listar por dependencia M12, marcar con política D2: suscripción → sin reintento) y
  `BdpPushFlushService` (worker con cadena de guards arming→backup→auditoría, no-op en standalone,
  dispatcher por dominio/operación). Endpoint manual `POST /api/bdp/push/flush` (Admin).
- **F2–F7 (catálogo/stock/departamentos/comandas/fidelización):** catálogo con 20 endpoints nuevos
  + `BdpWeblinkClient` con 20 métodos + structs PascalCase. Wiring en handlers locales: artículo
  (D3 rango reservado `bdp_articulo_rango_inicial`, default 90 000 000), departamento/familia
  (D7 código secuencial), propina por venta (D8, `ventas.propina`), puntos de fidelización (D9,
  ledger `bdp_puntos_cliente`) e inventario físico (D6=A, `POST /api/bdp/inventario` →
  `UpdateMassiveInventory`). Migración `20260823000000_bdp_catalogo_propina_puntos`.
- **F6 — CancelOrder como push:** `payload_cancelar` (`OrderIdentifier { OrderId }`, M26) y encolado
  `venta/cancelar` desde `VentaService::anular` cuando la venta anulada (anulación fresca, no
  reintento idempotente) tiene `bdp_order_id` (M16). El worker lo despacha vía `cancel_order`; si la
  suscripción rechaza queda `pendiente_suscripcion` con reintento solo manual (D2). En standalone no
  se envía nada (no-op del worker).
- **UI (F4–F7 + D10):** botón "Llamar camarero" en el plano (oculto en standalone), propina por
  venta (sumar/sustituir), puntos en ficha de cliente, catálogo de departamentos/familias,
  inventario (esperadas vs contadas, envío por lotes) y botón "Sincronizar a BDP" en el header
  (flush manual D1/D2).
- **Simulador + pruebas:** simulador Python ampliado con rutas nuevas + inyección de fallos por
  suscripción (99 tests). Tests Rust: payloads encolados + endpoint de inventario + invariante
  standalone no-envía + camino feliz end-to-end (wiremock: método/path/payload + fila sincronizada).
- **Corrección de arming:** las CHECK originales de `bdp_write_arming` (scopes ⊆
  `create_order/create_customer/add_payment/invoice` y `target_entity_type IN ('venta','cliente')`)
  bloqueaban el arming del push con dominios/scopes nuevos. Migración
  `20260824000000_bdp_write_arming_ampliar` amplía ambas listas (15 scopes, 8 dominios).
- **Evidencia:** `cargo check --lib --tests` limpio; `cargo test --lib` **153 passed**;
  `cargo test --test bdp_push` **13 passed**; `bdp_inventario` **3 passed**; `bdp_f8_permisos`
  **24 passed**; `bdp_service_integration` **8 passed** (3 ignorados = simulador); `cargo fmt --check`
  limpio; clippy sin warnings. Frontend `tsc --noEmit` limpio.
- **Archivos:** `src/services/bdp_push.rs`, `bdp_write_guard.rs`, `bdp_weblink.rs`,
  `bdp_weblink_catalog.rs`, `bdp_sync.rs`, `bdp_order_poller.rs`, `venta.rs`, `modo_operacion.rs`;
  `src/handlers/{bdp_push,bdp_article_map,bdp_catalogo,ventas,clientes,plano_sala,configuracion}.rs`;
  `src/models/*`, `src/repositories/*`; `migrations/20260821*…20260824*`; tests `bdp_push.rs`,
  `bdp_inventario.rs`; frontend `api/bdp.ts`, `components/site-header.tsx`,
  `componentes/{PlanoSala,venta-row-actions,PuntosCliente,bdp/BdpCatalogo,bdp/BdpInventario}.tsx`.
- **Pendiente real (diferido por diseño, no bloqueante):** verificación real contra BDP
  (suscripción/datos del cliente; BDP offline). `CancelOrder` real queda sujeto a suscripción activa
  (D2: reintento solo manual vía botón "Sincronizar a BDP").

## Cierre de la deuda M1/M2/M3 de 128A-1

- **M1 (invariantes del conmutador):** se eliminaron los checks redundantes de `bdp_sync_enabled`/
  `bdp_configurado` que contradecían el switch maestro `modo_operacion` (el caso `modo_operacion='bdp'`
  forzado con `bdp_sync_enabled=false` quedaba bloqueado). Afectados: `bdp_sync.rs`
  (sync_venta/add_order_payment/invoice_order), `venta.rs` (retry_bdp_sync), `ventas.rs`
  (obtener_bdp_status), `bdp_write_guard.rs` (try_auto_arm/armar_push) y `bdp_order_poller.rs`
  (SQL de `poll_due` + `poll_pending`). El modo efectivo es ahora la única puerta.
- **M2 (histéresis):** ya implementada en `ServicioModoOperacion` (umbral 3, `registrar_fallo_bdp`/
  `registrar_exito_bdp`) y cableada al poller; se añadió el cableado a las escrituras directas
  (`bdp_payment`/`bdp_invoice` bloquean en degradación con mensaje claro y alimentan la histéresis
  con éxito/fallo).
- **M3 (cache TTL/invalidación):** ya tenía consumidores (`invalidar` en PATCH de config;
  `modo_efectivo` con TTL en `bdp_purchase_note`/`configuracion`/`modo_operacion`); solo estaba mal
  etiquetada como "sin consumidores" en el roadmap.
- **Evidencia:** `cargo check --lib --tests` limpio; `cargo test --lib` **153 passed** (incluye tests
  M2 en `modo_operacion.rs`); suite de integración sin fallos.

## 198A-2 — Pruebas de interfaz: independencia + escritura (sin BDP real, completado)

- **Qué:** verificación a nivel de interfaz (navegador + backend local + BD de rama, sin BDP real)
  de que la independencia (128A-1) y los efectos locales de la escritura (198A-1) funcionan en
  `standalone` con **cero tráfico a BDP**.
- **Entorno aislado:** backend `:3100` (`PORT=3100` + `run-with-db.mjs`, BD de rama
  `glory_backend_glory_rs_rest`) + Vite `:5180` (proxy configurable `VITE_API_TARGET`), seed demo;
  sin tocar los servidores ajenos en `:3000`/`:5174`. `modo_efectivo=standalone` verificado.
- **Primera pasada:** badge off ✅, "Sincronizar a BDP" oculto ✅, CallWaiter oculto ✅,
  departamento local con código secuencial ✅, inventario renderiza ✅, banners de desactivación ✅,
  cero tráfico BDP ✅. **Bug corregido:** bucle infinito `Maximum update depth exceeded` en
  `BdpStatusIndicator` (`site-header.tsx`) por literal `{ status, data }` nuevo por render → memoizado.
- **Segunda pasada:** propina end-to-end (diálogo → `5.50 €` en `ventas.propina`) ✅, anulación local
  end-to-end (motivo + confirmación → `anulada=true` + `anulacion_motivo`) ✅, Compras (4 albaranes,
  sync deshabilitado, nuevo albarán) ✅, Explorador (menús/packs locales + 4 definiciones BDP) ✅,
  Historial (auditoría + snapshots; registra `anular_venta` Local) ✅, Stock (sync deshabilitado) ✅,
  **403 permisos** (trabajador → `POST /api/bdp/push/flush` y `GET /api/trabajadores` 403 claro;
  lectura de configuración 200) ✅, cero tráfico BDP (solo localhost) ✅.
- **Evidencia:** plan `plan-pruebas-interfaz-bdp-2026-08-19.md` con tabla 7b/7c por caso;
  roadmap `198A-2` marcado completado. Sin cambios de código de negocio en esta pasada (solo el
  fix de `site-header.tsx`).

## Decisiones y gotchas

- **D1–D10 de 198A-1** resueltas (ver plan); destacan: D1 `push_modalidad` configurable
  (default `automatico`, botón manual siempre), D2 bloqueo por suscripción → reintento **solo manual**,
  D3 código de rango reservado, D4 `AllProfiles=true`, D6 inventario completo, D7 código secuencial,
  D8 propina sumar/sustituir, D9 gating por módulo, D10 CallWaiter incluido.
- **Gotcha 1 (arming):** `sqlx::migrate!` embebe las migraciones en compilación y no detecta
  archivos nuevos sin forzar recompilación (`touch` del test) cuando el binario estaba cacheado.
- **Gotcha 2 (CHECK `bdp_write_arming`):** las CHECK restrictivas (scopes/dominios) de la fase de
  solo-lectura bloquearon silenciosamente el arming del push; el test end-to-end lo destapó.
- **Gotcha 3 (entorno):** `run-cargo.mjs` a secas compila contra la BD obsoleta de `.env`
  (`glory_db`); usar `run-with-db.mjs` (BD de rama `glory_backend_glory_rs_rest`). El enlazado PDB de
  Windows puede agotar disco si se fuerza rebuild; limpiar `C:\tmp\glory-target\<rama>`.
- **Todo sin commitear** (no se commiteó sin autorización; el git del repo quedó pendiente de otro
  agente según el usuario).
