# Tareas completadas — 2026-08-27

## Bloque 208A-2 — Corrección de la independencia BDP (H1–H8, decisiones D1–D6)

Plan archivado: `Agente/planes/completados/plan-correccion-independencia-bdp-2026-08-27.md`.
Origen: auditoría 208A-1 (`plan-auditoria-independencia-bdp-2026-08-27.md`, §6 con las decisiones
D1–D6 tomadas por el usuario).

### Qué se hizo (por fase)

- **C1 (H1/D1/D6)** — CRUD de artículos movido a la página **"Catálogo"** (pestañas Artículos /
  Departamentos y familias). Configuración → BDP queda solo con conexión/mapeos/permisos y un
  enlace "Ir a Catálogo". `config-bdp-mapeos.tsx` reducido a mapeos técnicos.
- **C2 (H2/H7/D2)** — Botón **"Nuevo artículo"** en Stock (`NuevoArticuloDialog.tsx`, reutiliza
  `POST /api/bdp/article-maps`), empty state accionable, y **"Sync catálogo"/"Sync precios"
  deshabilitados** fuera de modo bdp (H7, también en `BdpArticleCatalogActions`).
- **C3 (H3/H4/D3/D4)** — Persistencia local del conteo de inventario: migración
  `20260827000000_bdp_conteos_inventario` (cabecera fechada + líneas + clave de idempotencia),
  endpoints `GET/POST /api/bdp/inventario/conteos` y `GET .../:id`; el guardado **aplica la
  diferencia al stock local** con motivo 'conteo' y auditoría idempotente, y encola el envío solo
  para líneas con código BDP. UI: "Guardar conteo", observaciones, historial con "Retomar" y
  mensaje honesto en standalone (sin toast engañoso).
- **C4 (H6/D5)** — Sección **"Sincronización"** (`/bdp/sincronizacion`, menú): `GET
  /api/bdp/push/pendientes` (fila con estado/reintentos/último error) y `POST
  /api/bdp/push/:id/reintentar` (reintento manual, respeta D2; no-op en standalone). Acciones
  deshabilitadas fuera de modo bdp.
- **C5 (H5)** — Normalización en `PATCH /api/configuracion`: `standalone` + `sync=true` se
  fuerza a `sync=false` (estado no contradictorio).
- **C6 (H8)** — Empty state de Compras ofrece "Nuevo albarán" como primera acción.
- **Regla AGENTS.md (solicitud del usuario)** — `no-heavy-suites` en el AGENTS.md raíz: prohibe
  suites completas por defecto; test por test de lo necesario.

### Evidencia

- `cargo check --lib --tests` exit 0 (con BD de rama vía `scripts/run-with-db.mjs`).
- Tests nuevos, ejecutados **uno a uno**: `bdp_conteos_inventario` 4/4, `bdp_push_cola` 5/5,
  `bdp_config_normalizacion` 3/3.
- Regresión: `bdp_inventario` 3/3, `bdp_push` 13/13.
- `npm run type-check` (frontend) limpio.
- UI (preview `:5180`, backend `:3100`, BD de rama, seed demo, standalone): Catálogo unificado
  (alta TEST-1 local, rango reservado 90000000); Stock con "Nuevo artículo" y Sync deshabilitado;
  Inventario end-to-end (contadas 5 → stock local TEST-1 = 5, historial "aplicado"); Sincronización
  con cola visible (inventario, TEST-1, departamento — todos pendientes) y botones deshabilitados;
  Compras con empty state nuevo; Configuración → BDP sin CRUD; **cero tráfico a BDP** (solo
  localhost en logs de red).

### Decisiones del usuario aplicadas

D1 mover CRUD a Catálogo · D2 alta en Stock · D3 persistir conteos · D4 aplicar diferencia al
stock · D5 sección Sincronización · D6 Configuración solo conexión/mapeos/permisos.

### Pendientes reales (no bloquean, diferidos por diseño)

- Verificación real contra BDP (suscripción WebLink pendiente de activar por el cliente hasta
  24/09/2026): lecturas 138A-2 y escrituras (pago/factura/CancelOrder).
- Deploy a producción (requiere autorización).
