# Tareas completadas — 2026-07-15

## 157A-7 — F9.1 ExportArticles: Sync de catálogo BDP → Glory

- **Qué:** Implementación completa del endpoint `POST /api/bdp/article-maps/sync-catalog`. Lee catálogo de BDP vía `ExportArticles`, sincroniza con `bdp_article_map` usando upsert por `(user_id, articulo_glory_codigo)` con detección de cambios reales (IS DISTINCT FROM).
- **Archivos:**
  - `migrations/20260715200000_bdp_article_map_enriched.{up,down}.sql` — ALTER TABLE con 9 columnas nuevas
  - `src/models/bdp_article_map.rs` — campos enriquecidos en struct
  - `src/services/bdp_weblink_catalog.rs` — structs `BdpExportArticleItem`, `BdpExportArticlesResponse`, helper `default_true()`
  - `src/services/bdp_sync.rs` — método `sync_catalog()` + `BdpCatalogSyncResult`
  - `src/services/mod.rs` — exporta `BdpCatalogSyncResult`
  - `src/repositories/bdp_article_map.rs` — `upsert_from_bdp()` con `BdpArticleUpsertData` (refactorizado a struct por clippy too_many_arguments)
  - `src/repositories/mod.rs` — exporta `BdpArticleUpsertData`
  - `src/handlers/bdp_article_map.rs` — handler `sync_catalog` + route
- **Gotchas:** Clippy `too_many_arguments` (11 params → refactorizado a struct). Clippy `doc_markdown` (backticks en docs de ExportArticles/ItemCode). `cargo fmt` aplicado.
- **Sentinel:** No aplica
- **GLORY:** No aplica (endpoint específico de BDP, no reutilizable)
