# 2026-09-17 — 169A-2 nocturna: P0 + P1 en vivo (cero creación)

## P0 solo-lectura — PASS (19:0x, BDP real)
- `GET /api/configuracion/bdp/sync-dry-run` como `demo@restaurante.com` (admin):
  configurado=true, listo=true, escritura_real=false.
- BDP: `IsAlive=true`, login OK, versión **36.2 Hostelería** (coincide hallazgo 89
  de Guillermo). 557 artículos, 10 tenders en POS, 3 empleados. Cero errores de
  licencia. Evidencia: `C:\tmp\glory-p0.json`.

## P1 OnlyCheck con payload de pago — PASS (BDP real, cero creación)
- Cableado previo (sin commit): `execute(..., con_pago_tender)` +
  `?tender_id=` opt-in en el endpoint (solo dueño), tender validado contra la
  lista del POS (tender 99 → error local sin red; test con wiremock).
- `GET .../sync-dry-run?tender_id=1` → check **"CreateOrder OnlyCheck con pago" OK**:
  `OrderOperationType=1`, `OrderEndType=1`, `Invoice=true`,
  `Payments=[{TenderId:1, Amount:5, PaymentId:GDRY1789687349}]`.
- **Conclusión: la gratuita ACEPTA validar pago-en-creación** (sin error de
  licencia ni de validación). No se creó nada (OperationType=1).
  Evidencia: `C:\tmp\glory-p1.json`.
- Tests: `cargo test --lib` filtros 34/34 + `clippy --lib -D warnings` limpio.

## P2 real mínima — PENDIENTE (requiere "go" explícito + sala vacía)
- Decisión abierta: `Invoice=true` genera factura real de 0,11 € (¿`CancelOrder`
  la anula limpio o deja residuo?). Propuesta: P2 con `Invoice=false`.
