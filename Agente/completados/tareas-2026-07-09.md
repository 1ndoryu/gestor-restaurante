# Tareas completadas — 2026-07-09

## 157A-9 — Fase 9: Catálogo BDP completo (F9.1 tests + F9.2-F9.5 implementación)

- **Que:**
  - F9.1: 5 tests nuevos para `upsert_from_bdp` (crear, actualizar, sin_cambios, desactivar, aisla_usuarios). Total: 17 tests pasando.
  - F9.2: `resolve_article()` enriquece datos con `GetArticle` (nombre, precio, IVA). Fallback a config si falla.
  - F9.3: `BdpSyncService::sync_prices()` + handler `POST /api/bdp/article-maps/sync-prices`. Actualiza `precio_tarifa1` de artículos mapeados.
  - F9.4: `BdpSyncService::sync_tables()` + handler `POST /api/bdp/sync-tables`. Crea `ZonaSala` y `Mesa` desde `GetRoomsTables`.
  - F9.5: Handlers para `GET /api/bdp/menus/:id`, `/fastfoods/:id`, `/packs/:id`. Devuelven JSON raw de BDP.
  - 7 nuevos endpoints registrados en router.
  - 11 tipos nuevos en catalog (request/response structs), 7 client methods.
- **Archivos:**
  - `src/services/bdp_weblink_catalog.rs` — 8 path constants, 2 enum variants, 7 endpoint specs, 11 request/response structs
  - `src/services/bdp_weblink.rs` — 7 client methods + imports
  - `src/services/bdp_sync.rs` — resolve_article enrichment + sync_prices + sync_tables
  - `src/services/mod.rs` — exports nuevos (SyncTablesResult, BdpGetPricesArticlesResponse, etc.)
  - `src/handlers/bdp_article_map.rs` — 5 nuevos handlers + routes
  - `tests/bdp_article_map.rs` — 5 tests upsert_from_bdp
  - `roadmap.md` — F9.1-F9.5 marcadas completadas
- **Gotchas:**
  - `PlanoSalaRepository` usa funciones asociadas (`Struct::method`), no métodos (`self.method`).
  - `CrearMesaRequest` incluye `zona_id` como campo requerido, no como parámetro separado.
  - `CrearZonaRequest` tiene campos `Option<i32>` para orden/ancho/alto.
  - `validar_configuracion_bdp` no existe como función — se usa validación inline `config.bdp_base_url.is_empty()`.
  - `BdpGetRoomsTablesRequest` con `#[derive(Default)]` reemplaza manual impl (clippy derivable_impls).
  - `cargo clippy -- -D warnings` requirió: backticks en doc, `if let` en vez de `match` single-pattern, `try_from` para usize→u32.
- **Sentinel:** no aplica
- **GLORY:** No aplica — endpoints específicos del proyecto.
