# Plan 169A-5 — Escalar a 50.000 personas consultando con evidencia (caché + CDN + re-medición)

Objetivo: poder afirmar con evidencia medida que el producto sirve a 50.000
personas consultando a la vez. Estado de partida: informe PERF-VPS
(`Agente/completados/informe-perf-vps-2026-09-16.md`, matriz 9/9: T1 viable
para 1/10/3u, pico 70 req/s, p95 ~200 ms, cuello = postgres CPU).

## 0. La aritmética del claim (fija el DoD)

- 50.000 personas × 1 consulta / 10 s ≈ **5.000 req/s en el borde**.
- Con **≥95 % de aciertos en caché de borde**, al origen llegan ≤250 req/s.
- Origen: debe sostener 250 req/s con p95 < 800 ms, errores < 0,1 %, cero OOM.
- El claim cubre **consulta (lecturas)**; escrituras (venta/gasto) siguen al
  origen sin caché y no entran en la cifra.

## 1. Hechos verificados 2026-09-16 (no suposiciones)

1. **Cero caché hoy**: ningún `Cache-Control`/`ETag` en `src/` (grep vacío);
   respuestas sin headers de caché.
2. **Sin CDN en el path**: `perf.wandori.us` resuelve a la IP directa
   (66.94.100.241), respuestas sin `cf-ray`/`cf-cache-status` → registro
   gris (DNS-only). Activar proxy naranja + reglas es parte del trabajo.
3. **Índices base OK** (`ventas(user_id,fecha)`, `gastos_*`, `reservas_*`,
   `clientes_*`, etc.); la lentitud está en `ventas_listar` y agregados del
   dashboard → perfilar con `EXPLAIN` antes de asumir nada (D1).
4. **Manager**: `cache --action status|enable|disable` gestiona cache headers
   del sitio; DNS vía `switch-dns`/`setup-site-dns`. Sin comando visible para
   reglas de caché CF → si no puede activar proxy/reglas, fallback = panel
   Cloudflare manual (acción del usuario) o API CF con token.
5. Harness y muestreador existen (`scripts/perf/`); hay que añadir perfil
   lector, captura de `cf-cache-status` y más concurrencia.

## 2. Fases

### D1. Perfilado (solo lectura, staging). Sin código aún.

- [ ] D1.1 `EXPLAIN (ANALYZE, BUFFERS)` de `ventas_listar`, `dashboard_resumen`,
      `article_maps`, `article_stock` contra staging (vía `exec` + `psql`,
      solo lectura). Registrar planes + buffers + tiempos.
- [ ] D1.2 Clasificar endpoints en: cacheable-compartido (igual para todos los
      usuarios del restaurante: maps, stock, listados base), cacheable-privado
      (dashboard por usuario/rol), no-cacheable (login, escrituras, config).
- [ ] D1.3 Salida: tabla endpoint → clase + coste origen medido. Si un
      no-cacheable domina el perfil, se rediseña antes de seguir (no se tapa
      con caché lo que es un N+1).

### D2. Origen: abaratar lo que no se puede cachear

- [ ] D2.1 Índices que pida D1 (migración `up/down`, patrón del repo).
- [ ] D2.2 Caché en app (memoria, TTL corto 15–60 s) para agregados caros
      (dashboard/resumen por `(user_id, year, month)`), con invalidación en
      escrituras del mismo agregado. Sin Redis: un nodo, memoria local basta.
- [ ] D2.3 `Cache-Control: public, max-age=N` / `private` + `ETag` según clase
      D1.2. Auth (`Authorization: Bearer`) nunca forma parte de la clave de
      caché compartida; lo compartido se clavea por restaurante, no por usuario.
- [ ] D2.4 Criterio de salida: coste origen por lectura cacheable-privada
      ≤ 30 ms p50 en staging; re-medir con harness base@T1 antes de seguir.

### D3. Borde: Cloudflare delante

- [ ] D3.1 Activar proxy naranja en `perf.wandori.us`; verificar `cf-ray` en
      respuestas. (Vía manager si puede; si no, usuario en panel CF.)
      Verificar antes los límites del plan CF contratado (nº de reglas de
      caché disponibles); si no caben, pedir ampliación o consolidar reglas.
- [ ] D3.2 Reglas de caché: `GET /api/article-maps`, `/api/article-stock`,
      listados base → Edge Cache TTL 30–120 s, `stale-while-revalidate`;
      login/escrituras/config → bypass total. Clave de caché sin `Authorization`.
- [ ] D3.3 Criterio de salida: `cf-cache-status: HIT` ≥ 95 % en tráfico lector
      sintético; respuestas privadas jamás servidas desde caché (test negativo
      con 2 usuarios distintos: lo de A no lo ve B).

### D4. Harness "consulta50k" (versionado)

- [ ] D4.1 Nuevo escenario: 95 % GETs (reparto lector real), 5 % escrituras;
      N usuarios configurables (objetivo de diseño: 500–1000 virtuales sin
      pausa ≈ 5.000 req/s con p50 ~120 ms → validar por tramos).
- [ ] D4.2 Capturar `cf-cache-status` por petición: reportar HIT/MISS/EXPIRED +
      latencia edge vs origen por separado.
- [ ] D4.3 Rampa por tramos (100 → 250 → 500 → 1000 virtuales) con parada
      automática si p95 > 800 ms o errores > 0,1 % (no quemar el staging).

### D5. Medición y claim

- [ ] D5.1 Matriz reducida: `consulta50k` por tramos × (T1, T2) + origen-directo
      (bypass CF) como control. Muestreador app+pg como en 169A-4.
- [ ] D5.2 Informe individual `Agente/completados/informe-50k-YYYY-MM-DD.md`:
      req/s borde, % HIT, req/s origen, p95 edge/origen, CPU/RSS origen,
      traducción a personas (×1 req/10 s) **solo hasta el tramo verde medido**.
- [ ] D5.3 Regla de honestidad: la cifra publicada = tramo máximo superado con
      evidencia, redondeada hacia abajo. Si no llega a 50k, el informe dice
      cuánto sí y qué lo frena (siguiente cuello), no se infla.

## 3. Riesgos y límites

- **Datos viejos en caché**: TTL cortos + invalidación en escritura; el plan
  prefiere sub-cachear (menos HIT) a sobre-cachear (dato ajeno o viejo).
  El test negativo D3.3 es bloqueante.
- **JWT y privacidad**: nada con `Authorization` entra en clave compartida;
  endpoints por-usuario → `private` o bypass.
- **Staging ≠ producción**: el claim vale para esta carga sintética lectora;
  el informe lo dirá explícitamente (dataset, TTLs, fecha, commits).
- **Alcance**: solo staging `restaurante-perf`; cero BDP (standalone, regla
  oro 149A-2 intacta); ~35k filas `perf-harness` ya existentes + las nuevas,
  solo en staging.

## 4. Autorizaciones (nada se ejecuta sin ellas)

1. Push de `main` con D2 (código) — o desplegar con código viejo (invalida D5).
   Nota: el repo sigue público (flip a privado pendiente del usuario); el
   deploy necesita repo público o token — resolver antes de D5.
2. `deploy --update` en `restaurante-perf` tras D2.
3. Activar proxy CF + reglas (manager o panel manual por el usuario).
4. Tramos de carga alta D5 (picos de 5.000 req/s contra el borde; el origen
   solo ve ≤250 req/s por diseño, pero se pide igual).
5. Publicar la cifra (redacción del claim con la evidencia delante).

## 5. DoD

Tramo verde medido con `consulta50k` (HIT ≥ 95 %, origen p95 < 800 ms,
errores < 0,1 %, cero OOM) + informe individual con la cifra redondeada hacia
abajo + roadmap actualizado + decisión de staging (C.5 de 169A-4, aún
pendiente) registrada. Sin tramo verde no hay cifra.
